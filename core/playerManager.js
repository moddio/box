import { Mesh } from "@babylonjs/core/Meshes/mesh";

class PlayerManager {
  constructor() {
    this.players = {};
  }

  createPlayer() {
    // Player Setup
    let player = id;
  }

  getPlayer(id) {
    return this.players[id];
  }
}

export default PlayerManager;
